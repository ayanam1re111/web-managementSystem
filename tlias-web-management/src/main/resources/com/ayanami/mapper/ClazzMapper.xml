<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ayanami.mapper.ClazzMapper">
    <select id="list"  resultType="com.ayanami.pojo.Clazz">
        SELECT
        c.*,
        e.name masterName,
        CASE
        WHEN CURDATE() > c.end_date THEN '已结课'
        WHEN CURDATE() &lt; c.begin_date THEN '未开班'
        ELSE '在读中'
        END AS status
        FROM clazz c left join emp e on c.master_id=e.id
<where>
    <if test="name !=null and name !=''">
        c.name like concat('%',#{name},'%')
    </if>
    <if test="begin !=null">
        and c.begin_date=#{begin}
    </if>
    <if test="end !=null">
        and c.end_date=#{end}
    </if>

</where>

    </select>
</mapper>